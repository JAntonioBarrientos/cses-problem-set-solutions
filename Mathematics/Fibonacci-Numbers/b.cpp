#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const int MOD = 1e9+7;

template <int N, int M>
struct Matrix {

    ll mat[N][M];

    Matrix(){
        memset(mat, 0, sizeof(mat));
    }

    Matrix<N,M> operator+(const Matrix<N,M> &other) const{



};

