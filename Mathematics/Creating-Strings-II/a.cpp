#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    string s;
    cin >> s;
    vector<int> freq(26);
    for(char c: string){
        freq[c-'0']++;
    }

   
    return 0;
}
